apiVersion: apps/v1
kind: Deployment
metadata:
  name: chat-service
  namespace: social-media
  labels:
    app.kubernetes.io/name: chat-service
    app.kubernetes.io/component: application
  annotations:
    app.kubernetes.io/source-path: "chat-service"
spec:
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app.kubernetes.io/name: chat-service
      app.kubernetes.io/component: application
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        app.kubernetes.io/name: chat-service
        app.kubernetes.io/component: application
    spec:
      terminationGracePeriodSeconds: 30
      securityContext:
        seccompProfile:
          type: RuntimeDefault
      initContainers:
        - name: wait-for-postgres
          image: busybox:1.36
          command:
            - sh
            - -c
            - |
              until nc -z chat-db 5432; do
                echo "waiting for postgres at chat-db:5432";
                sleep 3;
              done
          securityContext:
            allowPrivilegeEscalation: false
            runAsNonRoot: true
            runAsUser: 1000
            runAsGroup: 1000
            capabilities:
              drop:
                - ALL
      containers:
        - name: chat-service
          image: social-media/chat-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 4004
          envFrom:
            - configMapRef:
                name: chat-service-config
            - secretRef:
                name: chat-service-secret
          env:
            - name: PORT
              valueFrom:
                configMapKeyRef:
                  name: chat-service-config
                  key: PORT
            - name: SERVICE_NAME
              valueFrom:
                configMapKeyRef:
                  name: chat-service-config
                  key: SERVICE_NAME
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: chat-service-secret
                  key: DATABASE_URL
          readinessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 15
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 6
          livenessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 45
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 6
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          securityContext:
            allowPrivilegeEscalation: false
            runAsNonRoot: true
            runAsUser: 1000
            runAsGroup: 1000
            capabilities:
              drop:
                - ALL
