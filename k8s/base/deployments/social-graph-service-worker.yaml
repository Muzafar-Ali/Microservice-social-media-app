apiVersion: apps/v1
kind: Deployment
metadata:
  name: social-graph-service-worker
  namespace: social-media
  labels:
    app.kubernetes.io/name: social-graph-service-worker
    app.kubernetes.io/component: worker
  annotations:
    app.kubernetes.io/source-path: "social-graph-service"
spec:
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app.kubernetes.io/name: social-graph-service-worker
      app.kubernetes.io/component: worker
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        app.kubernetes.io/name: social-graph-service-worker
        app.kubernetes.io/component: worker
    spec:
      terminationGracePeriodSeconds: 30
      securityContext:
        seccompProfile:
          type: RuntimeDefault
      initContainers:
        - name: wait-for-postgres
          image: busybox:1.36
          command:
            - sh
            - -c
            - |
              until nc -z ${DB_HOST} 5432; do
                echo "waiting for postgres at ${DB_HOST}:5432";
                sleep 3;
              done
          env:
            - name: DB_HOST
              valueFrom:
                configMapKeyRef:
                  name: social-graph-service-config
                  key: DB_HOST
          securityContext:
            allowPrivilegeEscalation: false
            runAsNonRoot: true
            runAsUser: 1000
            runAsGroup: 1000
            capabilities:
              drop:
                - ALL
      containers:
        - name: social-graph-service-worker
          image: social-media/social-graph-service:latest
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: social-graph-service-config
            - secretRef:
                name: social-graph-service-secret
          env:
            - name: SERVICE_RUNTIME_ROLE
              value: "worker"
            - name: SERVICE_NAME
              value: "social-graph-service-worker"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: social-graph-service-secret
                  key: DATABASE_URL
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          securityContext:
            allowPrivilegeEscalation: false
            runAsNonRoot: true
            runAsUser: 1000
            runAsGroup: 1000
            capabilities:
              drop:
                - ALL
