apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: social-media
  labels:
    app.kubernetes.io/name: user-service
    app.kubernetes.io/component: application
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: user-service
    app.kubernetes.io/component: application
  ports:
    - name: http
      port: 4001
      targetPort: http
