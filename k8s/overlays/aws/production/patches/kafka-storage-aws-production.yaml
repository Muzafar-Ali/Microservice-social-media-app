apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: kafka
  namespace: social-media
spec:
  volumeClaimTemplates:
    - metadata:
        name: kafka-data
      spec:
        storageClassName: gp3
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: 30Gi
